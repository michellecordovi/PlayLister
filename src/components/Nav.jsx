import LoginButton from './LoginButton';
import ThemeToggler from './ThemeToggler';

function Nav() {
    return (
        <nav>
            <ThemeToggler/>
            <LoginButton/>
        </nav>
    )
}

export default Nav;